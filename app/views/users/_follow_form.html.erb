<% unless current_user?(@user) %>
  <div id="follow_form">
  <% if current_user.following?(@user) %>
    <%= render 'shared/follow/unfollow' %>
  <% else %>
    <%= render 'shared/follow/follow' %>
  <% end %>
  </div>
<% end %>